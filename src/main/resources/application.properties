spring.application.name=dineo

# Database configuration - Using external IP for local development
spring.datasource.url=jdbc:mysql://51.254.114.199:3306/dineo-project
spring.datasource.username=nael
spring.datasource.password=+Nael130797
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JWT Configuration
jwt.secret=dineoSecretKeyForJWTTokenGenerationAndValidation2024!@#DiNeo
jwt.access-token-expiration=1800000
jwt.refresh-token-expiration=2592000000

# Logging Configuration
logging.level.com.dineo_backend.dineo.authentication=INFO
logging.level.com.dineo_backend.dineo.authentication.controller=DEBUG
logging.level.com.dineo_backend.dineo.authentication.service=DEBUG
logging.level.root=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.file.name=logs/dineo-app.log
logging.file.max-size=10MB
logging.file.max-history=10


# ===================================================================
#                     EMAIL CONFIGURATION
# ===================================================================
# SMTP settings for password reset emails
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=atouuaninael@gmail.com
spring.mail.password=jfvv nwim vrty xbqe
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000

# ===================================================================
#                     SPRING BATCH CONFIGURATION
# ===================================================================
# Disable automatic job execution on startup
# Jobs will only run when explicitly triggered via JobLauncher
spring.batch.job.enabled=false

# Initialize Spring Batch database tables automatically
# This creates: BATCH_JOB_INSTANCE, BATCH_JOB_EXECUTION, etc.
spring.batch.jdbc.initialize-schema=always
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
mail.service=gmail


# ===================================================================
#                     BUNNY CONFIGURATION
# ===================================================================

bunny.api.url=https://storage.bunnycdn.com/dineo
bunny.api.key=ca5b1769-0d4d-442e-9cdf39a48f95-00ee-4836
bunny.cdn.url=https://dineo.b-cdn.net



# ===================================================================
#                     STRIPE CONFIGURATION
# ===================================================================
# Stripe API Keys - Set via environment variables
stripe.secret.key=${STRIPE_SECRET_KEY}
stripe.publishable.key=${STRIPE_PUBLISHABLE_KEY}
stripe.currency=eur


# ===================================================================
#                     OVH SMS CONFIGURATION
# ===================================================================
ovh.sms.application.key=fce41afb74fcc2d0
ovh.sms.application.secret=b58e1c57548407772a05fa63b81353c9
ovh.sms.consumer.key=31414bf201a4a4d7c8694d74cadca9fe
ovh.sms.service.name=sms-as450891-1
ovh.sms.api.endpoint=https://eu.api.ovh.com/1.0
ovh.sms.sender=MIAMLO